*.test
*.out
bench.txt

# Catalogue exports are inputs to `make catalog`, not part of the repository:
# they are large and belong to whoever published them. The generated
# internal/catalog is the artefact worth committing.
*.csv
*.json

# …except the published lookup API, which is generated but is the site: those
# files are what GitHub Pages serves.
!api/*.json
